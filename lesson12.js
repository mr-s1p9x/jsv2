// Арифметические операторы

const width = 10;
const height = 5;
const space = width * height;
const newWidth = width - 4;
const newWidth2 = width + 4;
const division = newWidth / newWidth2;
const volume = 2 ** 3; // 2 в степени 3, 2 * 2 * 2
console.log(volume);

// Строки
const city = 'Kiev';
const street = 'Street';
console.log(city + ', ' + street + ' ' + 12); // Конкатенация строк
